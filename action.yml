name: 'Auto tag increment'
description: 'Add one more to the last number of the tag'
inputs:
  token:
    description: 'New tag'
outputs:
  tag:
    description: 'New tag'
runs:
  using: 'node12'
  main: 'index.js'
branding:
  icon: 'tag'
  color: 'yellow'
